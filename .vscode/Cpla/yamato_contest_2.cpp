#include <bits/stdc++.h>
using namespace std;

int main()
{
	int N;
	cin >> N;

	vector<string> S(N);
	for (int i = 0; i < N; i++)
	{
		cin >> S.at(i);
	}

	for (int i = 0; i < N; i++)
	{
		if (S.at(i) == H || S.at(i) == D || S.at(i) == C || S.at(i) == S)
		{
			if(S.at(i+1) == )
		}
	}
}